# Security upgrade (branch dep-upgrade): patched image-parse + Streamlit CVEs.
# Streamlit 1.35 capped pillow<11, so patching Pillow forced a Streamlit bump.
# Keep Streamlit Cloud pinned to Python 3.12 — 3.13/3.14 lack prebuilt wheels.
streamlit==1.54.0
streamlit-option-menu
pandas==2.2.2
plotly==5.22.0
numpy==1.26.4
statsmodels

# Persistence (db.py). SQLAlchemy speaks both SQLite (local default) and
# Postgres (Supabase in production) from one code path.
SQLAlchemy>=2.0,<3
psycopg2-binary==2.9.9

# Image + OCR intake (app.py). Pillow/pillow-heif upgraded to patch a large
# batch of image-parsing CVEs (the app decodes user-uploaded HEIC/JPG/PNG, so
# these are the real attack surface). google-genai is the model SDK the OCR
# path imports (`from google import genai`).
Pillow==12.3.0
pillow-heif==1.3.0
google-genai==1.38.0
# protobuf: patch PYSEC-2026-1805 (transitive via google-genai / streamlit).
protobuf>=5.29.6
# Pin pydantic to a stable release — the resolver otherwise pulled a
# 2.14 alpha for google-genai, which we don't want on Streamlit Cloud.
pydantic>=2.7,<2.14
